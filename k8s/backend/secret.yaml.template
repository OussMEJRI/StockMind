# This is a template. Create the actual secret with:
# kubectl create secret generic backend-secret --from-literal=POSTGRES_PASSWORD=<password> --from-literal=SECRET_KEY=<jwt-secret> -n it-inventory

apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
  namespace: it-inventory
type: Opaque
data:
  # Base64 encoded values - replace with actual values
  # POSTGRES_PASSWORD: <base64-encoded-password>
  # SECRET_KEY: <base64-encoded-jwt-secret>
  # LLM_API_KEY: <base64-encoded-llm-api-key> (optional)
